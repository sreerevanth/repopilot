node_modules

# Agent tool bundles, generated by ecc-tools and refreshed wholesale rather
# than edited by hand. Prettier was failing on every pull request because of
# them: a missing trailing newline in .codex/AGENTS.md, and a genuine YAML
# syntax error in the homunculus instincts file that no reformatting fixes.
# Formatting generated output is not something a contributor can act on.
.agents/
.claude/
.codex/
